{
  "name": "core-schematics",
  "description": "A set of schematics to simplify the process of creating consumer pages for the DEMIS Portal-Core library",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "test": "vitest run"
  },
  "keywords": [
    "schematics"
  ],
  "author": "",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {},
  "devDependencies": {
    "vitest": "4.1.10",
    "@vitest/ui": "4.1.10"
  }
}
